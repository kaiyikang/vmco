#!/usr/bin/env sh
set -eu

ROOT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
JAR="$ROOT_DIR/target/reviewm-0.1.0-SNAPSHOT.jar"

if [ ! -f "$JAR" ]; then
  echo "reviewm jar not found. Build it first with: mvn -q -DskipTests package" >&2
  exit 1
fi

exec java -jar "$JAR" "$@"
