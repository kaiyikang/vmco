---
generated_at: $generated_at
repo_root: $repo_root
base_branch: $base_branch
merge_base: $merge_base
template_name: $template_name
template_version: $template_version
template_path: $template_path
---

# LLM Review Instruction

Read this file first. Use the facts below to review the current branch.

## Core goal

Summarize and review the core production behavior changes in this branch.

Main question:

> Are the production behavior changes protected by tests?

Do not perform a broad style review.

Focus on:

- behavior changes
- missing tests
- weak assertions
- regression risk
- API compatibility
- mapper / DTO / entity field mistakes
- null / default behavior
- configuration impact

## Repository

```text
$repo_root
```

## Base branch

```text
$base_branch
```

## Merge base

```text
$merge_base
```

## Changed files overview

```text
$changed_files_overview
```

## Diff stat

```text
$diff_stat
```

## Core production/config files changed

$core_files_section

## Core files and possible related tests

$core_context_section

## Changed test files

Changed or added tests are supporting context. Review them for relevance to the
behavior changes above; do not treat their presence as proof of coverage.

$changed_test_files_section

## Output constraints for the LLM

Create one concise Markdown review report.

The report should be short and list-based.

For each finding, include:

- Severity: blocker / major / minor / nit
- Location: exact file + class / method / field if possible
- Changed behavior
- Problem
- Existing test coverage
- Missing or weak test
- Suggested fix or suggested test

Coverage rule:

A test is relevant only if it exercises the changed behavior and has an
assertion or observable outcome that would fail before this branch's change and
pass after it. Do not count filename matches, generic coverage, or unrelated
unit tests as sufficient coverage.

Changed-test relevance rule:

Evaluate changed or added tests for relevance to the changed production/config
behavior. Call out tests that add volume without exercising changed behavior as
weak, redundant, or unrelated. Do not criticize existing unmodified unrelated
tests unless they materially obscure this review.

Rules:

1. Point to concrete code locations so the developer can jump to the code quickly.
2. Prefer precise findings over generic advice.
3. Do not comment on formatting, import order, or naming unless it hides a real bug.
4. Do not list issues that are only speculative without explaining what evidence is missing.
5. If a changed behavior is already well covered by tests, say so briefly.
6. If no blocker or major issue is found, say that explicitly.
7. Keep the final report concise.

## Recommended LLM task

Inspect:

1. The changed production/config files listed above in their current state.
2. The merge-base-side snippets in this prompt as previous-behavior evidence.
3. The changed test files listed above.
4. The possible related tests listed above.

Produce one concise Markdown review report answering:

1. What core behavior changed?
2. Is each behavior change protected by tests?
3. What tests are missing or weak?
4. Are there any blocker or major risks?
5. What exact code or test changes should be made?
